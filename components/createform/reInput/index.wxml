<view class="form_item">
    <view class="form_title"><span class="red" wx:if="{{required}}">*</span>{{fieldName}}</view>
    <view class="control">
        <input type="text" placeholder="输入" name="{{fieldName}}" value="{{name}}" bindinput="nameChange" />
    </view>
</view>